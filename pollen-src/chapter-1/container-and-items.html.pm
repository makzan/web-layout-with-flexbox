#lang pollen

◊headline{Container and items}

At any moment, we should only focus on one container and its children items. Flexbox is all about the container and how items inside take the spaces. When dealing with the container-items pair, we don’t care if the item is a container of another set of elements.